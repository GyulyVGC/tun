[package]
name = "tun"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tun = "0.6.1"
once_cell = "1.19.0"
tokio = { version = "1.35.1", features = ["net", "sync", "rt-multi-thread", "macros"] }
